# 합류 대기판 — 승인된 구현 계약

사용자 승인: 별도 대기판 시안 → 2026-09-12 “오케이 구현해봐.”
기존 `codex/main-web-scope-20260908` 작업을 이어간다. HEAD 53e80426, 기존 더티 변경은 보존하며 이번 변경만 구분한다. 운영 적용·결제·Git stage/commit/push 승인이 아니다.

## 목표

- 모집방과 동급의 합류 대기판. 방을 만들기 전에도 허용된 학교·학과 안에서 후보와 정확한 대기 인원을 볼 수 있다.
- 한 사람은 여러 가능한 포지션을 등록하지만 인원은 한 명으로 집계한다. 개인 실력 순위가 아니다. 닉네임·공개 동의한 소개·가능 시간만 노출한다.
- 내 등록은 본인 화면에서 고정하며 등록 수정·취소·받은 초대가 분명하다. 폴링/화면 복귀 시 서버 상태를 다시 조회한다. 실제 조회 실패를 0명이나 가상 인원으로 대체하지 않는다.
- 실제 후보 등록 동의를 근거로 비친구에게도 주장이 자기 방의 특정 자리로 초대할 수 있다. 기존 친구 초대의 관계 제한은 없애지 않는다.
- 초대 수신은 대기를 유지. 참가 절차 선택 시 잠시 비노출. 취소·진행 기한 종료 시 현재 자격이 유효할 때만 복귀. 실제 멤버십 확정 시 같은 모집 단위의 후보 등록 종료와 인원 제외를 함께 처리한다.
- 이미 합류한 후보는 이전에 열린 화면의 초대/수락 요청도 서버에서 거절한다. 별도 과목·별도 활동의 등록까지 취소하지 않는다. 탈퇴 후 무동의 재등록하지 않는다.
- [2026-09-13 사용자 정정으로 폐기] 후보 등록 자체는 무료라는 이전 결정. 아래 선납 등록 계약을 우선한다. PG 미연결을 무료 가입으로 우회하지 않는다.

## 작업과 소유권

1. 서버 A: 별도 후보 계약과 forward-only SQL, PostgreSQL 실행 회귀. private 원장, 권한·현재 멤버십·중복·슬롯·집계 검사.
2. 서버 B: 인증된 HTTP, 공통 알림/현재 권한 resolver, 기존 리그·스터디·멘토링의 진입 링크.
3. 주 작업자: 승인 이미지에 맞춘 공통 대기판·등록 지도·받은 초대 UI, 로컬 전용 리허설, 회귀·브라우저·디자인 비교·보고.
4. 독립 검토: 고정된 이번 변경 파일과 의존 코드의 권한·데이터·실사용 흐름 점검. 기존 사용자 변경 되돌리지 않기.

API: `/api/meetups/candidates`, GET `scope_kind/scope_key/filter/cursor`, POST `{action,args}`, `{data:CandidateBoard}`. UI `/meetups/candidates?kind=league&key=lol`, 개발 리허설 `/meetups/dev-candidates`. 실제 페이지는 예시 데이터를 반환하지 않는다.

## 확인할 사례

- 미등록 → 복수 역할 등록 → 한 명 증가·내 카드 고정 → 수정/취소.
- 포지션 필터·더 보기·정확한 총인원, 중복 카드/중복 사람 없음.
- 초대 보내기·수신자 보기·거절·다른 초대 유지·참가 준비·준비 취소.
- 실제 SQL 멤버십 확정 후 목록·집계 제외, 오래된 초대 차단, 다른 scope 유지.
- 다른 계정/학과/학교·차단·계정 변경·응답 역전·연결 실패·중복 요청·슬롯 충돌.
- 모바일 390px 및 데스크톱에서 직접 클릭/캡처, 기존 실제 진입 경로 확인. 이미지 시안과 렌더 비교.

범위 밖: 원격 migration, 실제 Auth 계정 변경, 실제 결제/환불, 기기 발송, 운영 배포, 기존 매칭 정책 변경. 로컬 fixture와 리허설은 운영 완료 증거가 아니다.

## 2026-09-13 추가 승인 — 합류 종료 안내와 등록 전 보증금

### G1. 실제 합류 후 양쪽에 남는 안내

- R1: 실제 멤버십 확정 시 본인에게 가입한 모임과 다른 초대 자동 종료 안내. 수락 클릭이나 진행 중에는 완료라고 하지 않는다.
- R2: 종료된 다른 초대의 팀장에게 모집 종료 알림과 같은 활동의 다른 대기자를 찾는 연결. 합류한 팀 이름·다른 활동·금융 정보를 해당 팀장에게 공개하지 않는다.
- R3: 중복 이벤트/다중 역할 초대에도 불필요한 중복 알림 없음. 가입자는 채팅으로, 다른 팀장은 대기판으로 이어진다. 지나간 초대 이력은 상태와 함께 확인할 수 있다.
- 완료 근거: 실제 SQL 트리거 로컬 시험, 알림 권한/중복 시험, 양쪽 화면 및 모바일·데스크톱 캡처. 운영 발송은 별도 미검증.

### G2. 보증금 확인 후 대기 글 공개

- R4: 기존 지도·티어·소개 입력 뒤 별도 보증금 확인 단계. 서버에서 납부를 확인하기 전에는 새 글을 공개하지 않는다. 취소/실패/미연결은 공개·가입 성공이 아니다.
- R5: 향후 선납금은 본인·활동에 귀속되어야 하며 팀 합류 시 중복 청구 없이 연결되어야 한다. 다른 활동 금액을 무동의 사용하거나 원래 납부 출처를 바꾸지 않는다.
- 조사 결과: 현재 결제와 원장은 방 ID 필수이며 범용 잔액·후보 예약이 없다. 기존 매칭 carryover는 후보 결제에 전용할 수 없다. 따라서 실제 선납/이월이 된 것처럼 구현하지 않는다.
- 이번 안전한 구현 단위: 보증금 준비 계약/조회, 서버의 미납 신규 공개 차단, 결제 미연결 표시, 개발 전용 납부/공개/취소 체험. 기존 활성 등록의 수정/취소는 보존하되 납부 완료로 표시하지 않는다.
- 실제 수납·예약 이전은 미완료 경계: 2026-09-13 후속 승인으로 반환은 사용자의 별도 신청 후 처리한다. 대기 취소만으로 자동 환불하지 않는다. 금액·만료·차액 처리와 공급자 검증/원장 계약은 남아 있다. 운영 금액은 임의로 1만 원 설정하지 않는다. 예시 1만 원은 개발 체험에만 사용한다.
- 완료 근거: 미납 신규 공개 실패 및 데이터 불변, 계약 변조/계정/범위 검사, 보증금 앞뒤 화면·실패 복귀 직접 확인. 실제 PG·이월 완료 증거와 구분한다.

### 실행 및 범위

- 기존 격리 작업공간 계속 사용. 시작 HEAD `53e80426c542e0d6fd426c9dfc5e144fdefe8357`, dirty 419개, staged 없음. 보호된 기존 변경 보존.
- 주 담당: 후보 화면/데모/상태 이력/검증. 알림 담당: 새 종료 알림 SQL·계약·회귀. 결제 담당: 별도 준비 계약/API·SQL 공개 차단·회귀. 파일 소유권 분리.
- 테스트에서 누락을 먼저 재현하고 구현 후 회귀·타입·브라우저 확인. 고정 후보 독립 검토가 끝나기 전 전체 완료라고 하지 않는다.
- `artifacts/qa/20260913-candidate-prepay`에 기존 화면 소스 대비 변경 및 캡처·결과를 모은다.
- 사용자 구현 승인은 실제 결제/환불/원격 적용/Git commit/push/배포 승인이 아니다.

### 2026-09-13 로컬 결과

- G1 결과 안내: 새 SQL·공통 알림·양쪽 UI 반영, 로컬/독립 통합 시험 통과. 운영 발송 미검증.
- G2 등록 전 보증금: 소개→준비 확인 화면, 미납 신규 공개 차단 반영. 실제 수납·보관·팀 귀속·반환은 미구현. 현재 migration을 운영 적용하면 금융 연결까지 신규 공개가 차단되므로 배포 후보로 보지 않는다.
- 최종 관련 회귀78/78, 프로젝트 TypeScript exit0, 고정본 독립 UI38/38·알림12/12·통합1/1, 390×844/1440×900 브라우저 검증.
- 상세: `artifacts/qa/20260913-candidate-prepay/REPORT.md`. 실제 결제·원격 DB·실기기·Git 반영 없음.

### 2026-09-13 후속 결정 — 반환은 별도 신청으로 통일

사용자: “보증금을 돌려달라고 신청하면 돌려주는걸로 통일하자”.

- 대기 취소와 보증금 반환 신청은 다른 행동이다. 취소만으로 결제수단에 자동 반환하지 않는다.
- 사용자가 해당 활동의 반환 가능한 보증금을 선택해 반환 신청 → 접수/처리 중 → 공급자 확인 후 반환 상태 표시. 신청 접수를 반환 완료로 표현하지 않는다.
- 반환 신청 시 아직 공개 중인 해당 대기 글·진행 중인 제안은 종료/참가 상태와 일관되게 정리해야 한다. 같은 보증금을 반환하면서 유료 대기 상태를 유지하지 않는다. 다른 활동의 예치금·등록은 건드리지 않는다.
- 신청하지 않은 돈을 다른 활동에 자동 이월하지 않는다. 기존 명시동의 이월 및 출석·분쟁·몰수 기준을 이번 결정만으로 폐기하거나 확대하지 않는다.
- 반환 개시 방식의 제품 정책은 확정되었지만 실제 반환 신청 API·원장·공급자 처리 연결은 미구현이다. 현재 화면은 그 경계를 안내하며 가짜 신청 버튼이나 완료 상태를 만들지 않는다.
- 이 결정은 실제 결제/반환 실행·원격 적용·Git commit/push·배포 승인이 아니다.
