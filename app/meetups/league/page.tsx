// Keep old community URLs compatible while new discovery belongs to Meetups.
// Both routes share the feature flag, parsing and authenticated API consumer.
export { default } from '@/app/community/department/page'
